all:
  vars:
    ansible_user: 'titem'
    ansible_become: true
    ansible_become_method: sudo
    ansible_private_key_file: /home/titem/.ssh/ansible

docker:
  hosts:
    10.0.0.77:

gateway:
  hosts:
    10.0.0.77:

